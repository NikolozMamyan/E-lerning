{% extends 'base.html.twig' %}

{% block title %}Available Courses{% endblock %}

{% block body %}
  <section class="course-section">
    <h2 class="section-title">Available Courses</h2>
    <div class="course-grid">
      {% for course in courses %}
        <div class="course-card">
          <div class="course-thumbnail bg-aml">
            {% if access[course.id] is defined and access[course.id] %}
              <img src="{{ asset('uploads/courses/' ~ course.thumb) }}" alt="{{ course.title }}">
            {% else %}
              <div class="lock-overlay">
                <div class="lock-icon">
                  <svg viewBox="0 0 24 24">
                    <path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 
                      1.1.9 2 2 2h12c1.1 0 2-.9 
                      2-2V10c0-1.1-.9-2-2-2zm-6 
                      9c-1.1 0-2-.9-2-2s.9-2 
                      2-2 2 .9 2 2-.9 2-2 
                      2zm3.1-9H8.9V6c0-1.71 
                      1.39-3.1 3.1-3.1 1.71 0 
                      3.1 1.39 3.1 3.1v2z"/>
                  </svg>
                </div>
              </div>
              <img src="{{ asset('uploads/courses/' ~ course.thumb) }}" alt="{{ course.title }}">
            {% endif %}

            {# Badge durée dans un coin de l'image #}
            {% if course.videos|length > 0 %}
              <span class="duration-badge">
                {{ (course.videos|first.duration / 60)|round(0, 'floor') }}:
                {{ '%02d'|format(course.videos|first.duration % 60) }}
              </span>
            {% endif %}
          </div>

          <div class="course-info">
            <h3 class="course-title">{{ course.title }}</h3>

            {# Limiter la description à 120 caractères #}
            <p class="course-subtitle">
              {{ course.description|slice(0, 120) }}{% if course.description|length > 120 %}...{% endif %}
            </p>

            <div class="course-footer">
              <a href="{{ path('app_course_show', {id: course.id}) }}" class="see-course-btn">See Course</a>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </section>
{% endblock %}

{% block stylesheets %}
<style>
.duration-badge {
  position: absolute;
  bottom: 8px;
  right: 8px;
  background: rgba(7, 23, 23, 0.75); /* noir semi-transparent */
  color: #fff;
  font-size: 0.8rem;
  font-weight: 600;
  padding: 2px 6px;
  border-radius: 6px;
}
.course-thumbnail {
  position: relative;
  overflow: hidden;
}
</style>
{% endblock %}
